/**
 * Ambient declarations for build-time define constants injected by Vite
 * (electron.vite.config.ts renderer config). These identifiers are replaced
 * with string literals at bundle time; they are never real variables.
 */
declare const __APP_VERSION__: string;
