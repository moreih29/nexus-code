{
  "name": "nexus-code",
  "version": "0.1.0",
  "private": true,
  "description": "Nexus Code — Claude Code GUI orchestration workstation",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "bun run --filter '@nexus/server' --filter '@nexus/web' dev",
    "dev:all": "bun run --filter '@nexus/server' --filter '@nexus/web' dev & sleep 3 && bun run --filter '@nexus/electron' dev",
    "clean": "bun run --filter '*' clean"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
