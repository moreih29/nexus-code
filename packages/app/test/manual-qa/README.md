# Manual QA (Task 12)

- Checklist: `korean-release-checklist.md`
- Evidence templates + run bundles: `release-evidence/`

This manual gate is a release blocker until completed on both arm64 and x64 signed `.app` runs.
