{
  "name": "@nexus/electron",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/main.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsc && NODE_ENV=development electron dist/main.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexus/shared": "workspace:*",
    "electron": "^34.0.0"
  },
  "devDependencies": {
    "typescript": "*",
    "@types/node": "^22.0.0"
  }
}
